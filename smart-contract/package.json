{
  "name": "nero-fuse-pay-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Nero-Fuse-Pay on NERO Chain",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network nero",
    "deploy-testnet": "hardhat run scripts/deploy.js --network neroTestnet",
    "verify": "hardhat verify --network nero"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@nerochain/aa-contracts": "^1.0.0",
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "hardhat-deploy": "^0.11.34"
  }
}